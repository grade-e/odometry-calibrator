# odometry_calibrator

ROS 2 Python package for straight-line odometry scale calibration, including independent +x, -x, +y, and -y calibration for omni drive robots.

See [src/odometry_calibrator/README.md](src/odometry_calibrator/README.md) for build and usage instructions.
